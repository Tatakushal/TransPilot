import { useMemo, useState } from "react";
import { Eye, Pencil, Trash2, Route, Search } from "lucide-react";
import StatusBadge from "@/components/ui/StatusBadge";
interface Trip {id:string;vehicle:string;driver:string;route:string;cargo:string;status:string}
export default function TripTable(){const[trips,setTrips]=useState<Trip[]>([{id:"TR-1001",vehicle:"Volvo FH16",driver:"Alex",route:"Hyderabad → Bangalore",cargo:"12 Tons",status:"Dispatched"},{id:"TR-1002",vehicle:"Tata Prima",driver:"Robert",route:"Chennai → Kochi",cargo:"8 Tons",status:"Completed"},{id:"TR-1003",vehicle:"Ford Transit",driver:"Rahul",route:"Delhi → Jaipur",cargo:"5 Tons",status:"Draft"}]);const[q,setQ]=useState("");const filtered=useMemo(()=>trips.filter(t=>Object.values(t).join(" ").toLowerCase().includes(q.toLowerCase())),[trips,q]);function del(id:string){if(confirm(`Delete trip ${id}?`))setTrips(x=>x.filter(t=>t.id!==id))}return <div className="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-sm"><div className="flex flex-col gap-3 border-b border-slate-100 p-5 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-bold text-slate-900">Trip registry</h2><p className="mt-1 text-sm text-slate-500">{filtered.length} journeys in view</p></div><div className="relative w-full sm:w-72"><Search size={16} className="absolute left-3.5 top-1/2 -translate-y-1/2 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search trips..." className="h-10 w-full rounded-xl border border-slate-200 bg-slate-50 pl-10 text-sm outline-none focus:border-indigo-500 focus:bg-white"/></div></div><div className="overflow-x-auto"><table className="w-full min-w-[900px]"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-7 py-4 text-left">Trip</th><th className="px-5 py-4 text-left">Vehicle</th><th className="px-5 py-4 text-left">Driver</th><th className="px-5 py-4 text-left">Route</th><th className="px-5 py-4 text-left">Cargo</th><th className="px-5 py-4 text-left">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{filtered.length?filtered.map(t=><tr key={t.id} className="border-t border-slate-100 hover:bg-slate-50/70"><td className="px-7 py-5"><span className="rounded-lg bg-slate-100 px-2.5 py-1 font-mono text-xs font-bold text-slate-600">{t.id}</span></td><td className="px-5 font-medium text-slate-700">{t.vehicle}</td><td className="px-5 text-sm text-slate-600">{t.driver}</td><td className="px-5 text-sm text-slate-600">{t.route}</td><td className="px-5 text-sm text-slate-600">{t.cargo}</td><td className="px-5"><StatusBadge status={t.status}/></td><td className="px-5"><div className="flex justify-end gap-1"><button className="rounded-lg p-2 text-slate-400 hover:bg-indigo-50 hover:text-indigo-600" title="View"><Eye size={17}/></button><button className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 hover:text-slate-700" title="Edit"><Pencil size={17}/></button><button onClick={()=>del(t.id)} className="rounded-lg p-2 text-red-500 hover:bg-red-50" title="Delete"><Trash2 size={17}/></button></div></td></tr>):<tr><td colSpan={7} className="p-14 text-center"><Route className="mx-auto text-slate-300" size={34}/><p className="mt-3 font-semibold text-slate-700">No trips found</p><p className="mt-1 text-sm text-slate-400">Try another search term.</p></td></tr>}</tbody></table></div></div>}
