import { useEffect, useMemo, useState } from "react";
import { Eye, Pencil, Trash2, UserRound, RefreshCw } from "lucide-react";
import StatusBadge from "../ui/StatusBadge";
import { deleteDriver, getDrivers, type Driver } from "@/services/driverService";
interface Props { search:string; refreshKey:number; onEdit:(driver:Driver)=>void; onView:(driver:Driver)=>void; }
export default function DriverTable({search,refreshKey,onEdit,onView}:Props){const[drivers,setDrivers]=useState<Driver[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[deleting,setDeleting]=useState("");const load=()=>{setLoading(true);setError("");getDrivers().then(setDrivers).catch(e=>setError(e instanceof Error?e.message:"Unable to load drivers.")).finally(()=>setLoading(false))};useEffect(load,[refreshKey]);const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return q?drivers.filter(d=>[d.name,d.license,d.licenseCategory,d.status].some(x=>x.toLowerCase().includes(q))):drivers},[drivers,search]);async function remove(d:Driver){if(!window.confirm(`Delete ${d.name}? This cannot be undone.`))return;try{setDeleting(d.license);await deleteDriver(d.license);setDrivers(x=>x.filter(i=>i.license!==d.license))}catch(e){setError(e instanceof Error?e.message:"Unable to delete driver.")}finally{setDeleting("")}}return <div className="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 px-7 py-5"><div><h2 className="font-bold text-slate-900">Driver roster</h2><p className="mt-1 text-sm text-slate-500">{filtered.length} of {drivers.length} drivers</p></div><button onClick={load} disabled={loading} className="rounded-xl border border-slate-200 p-2.5 text-slate-500 hover:bg-slate-50"><RefreshCw size={17} className={loading?"animate-spin":""}/></button></div>{error?<div className="m-5 rounded-2xl bg-red-50 p-5 text-sm text-red-700">{error}</div>:<div className="overflow-x-auto"><table className="w-full min-w-[850px]"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-7 py-4 text-left">Driver</th><th className="px-5 py-4 text-left">License</th><th className="px-5 py-4 text-left">Safety</th><th className="px-5 py-4 text-left">Expiry</th><th className="px-5 py-4 text-left">Status</th><th className="px-5 py-4 text-right">Actions</th></tr></thead><tbody>{loading?<tr><td colSpan={6} className="p-14 text-center text-sm text-slate-500">Loading drivers...</td></tr>:filtered.length===0?<tr><td colSpan={6} className="p-14 text-center"><UserRound className="mx-auto text-slate-300" size={34}/><p className="mt-3 font-semibold text-slate-700">{drivers.length?"No drivers match your search":"No drivers yet"}</p></td></tr>:filtered.map(d=><tr key={d.license} className="border-t border-slate-100 hover:bg-slate-50/70"><td className="px-7 py-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-indigo-100 text-xs font-bold text-indigo-700">{d.name.split(" ").map(n=>n[0]).join("").slice(0,2)}</div><div><p className="font-semibold text-slate-800">{d.name}</p><p className="text-xs text-slate-400">{d.licenseCategory}</p></div></div></td><td className="px-5 font-mono text-sm text-slate-500">{d.license}</td><td className="px-5 text-sm font-semibold">{d.safety}%</td><td className="px-5 text-sm text-slate-500">{d.licenseExpiryDate}</td><td className="px-5"><StatusBadge status={d.status}/></td><td className="px-5"><div className="flex justify-end gap-1"><button onClick={()=>onView(d)} className="rounded-lg p-2 text-slate-400 hover:bg-indigo-50 hover:text-indigo-600" title="View"><Eye size={17}/></button><button onClick={()=>onEdit(d)} className="rounded-lg p-2 text-indigo-600 hover:bg-indigo-50" title="Edit"><Pencil size={17}/></button><button disabled={deleting===d.license} onClick={()=>remove(d)} className="rounded-lg p-2 text-red-500 hover:bg-red-50 disabled:opacity-50" title="Delete"><Trash2 size={17}/></button></div></td></tr>)}</tbody></table></div>}</div>}
