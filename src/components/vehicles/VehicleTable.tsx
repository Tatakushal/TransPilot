import { useEffect, useMemo, useState } from "react";
import { Eye, Pencil, Trash2, Truck, RefreshCw } from "lucide-react";
import StatusBadge from "@/components/ui/StatusBadge";
import { deleteVehicle, getVehicles } from "@/services/vehicleService";
import type { Vehicle } from "@/types/vehicles";
interface Props { search:string; refreshKey:number; onEdit:(v:Vehicle)=>void; onView:(v:Vehicle)=>void; }
export default function VehicleTable({search,refreshKey,onEdit,onView}:Props){const[vehicles,setVehicles]=useState<Vehicle[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[deleting,setDeleting]=useState("");const load=()=>{setLoading(true);setError("");getVehicles().then(setVehicles).catch(e=>setError(e instanceof Error?e.message:"Unable to load vehicles.")).finally(()=>setLoading(false))};useEffect(load,[refreshKey]);const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return q?vehicles.filter(v=>[v.registration,v.model,v.type,v.status].some(x=>x.toLowerCase().includes(q))):vehicles},[vehicles,search]);async function del(v:Vehicle){if(!window.confirm(`Delete vehicle ${v.registration}? This action cannot be undone.`))return;try{setDeleting(v.registration);await deleteVehicle(v.registration);setVehicles(x=>x.filter(i=>i.registration!==v.registration))}catch(e){setError(e instanceof Error?e.message:"Unable to delete vehicle.")}finally{setDeleting("")}}return <div className="overflow-hidden rounded-[28px] border border-slate-200 bg-white shadow-sm"><div className="flex items-center justify-between border-b border-slate-100 px-7 py-5"><div><h2 className="font-bold text-slate-900">Vehicle registry</h2><p className="mt-1 text-sm text-slate-500">{filtered.length} of {vehicles.length} vehicles</p></div><button onClick={load} disabled={loading} className="rounded-xl border border-slate-200 p-2.5 text-slate-500 hover:bg-slate-50"><RefreshCw size={17} className={loading?"animate-spin":""}/></button></div>{error?<div className="m-5 rounded-2xl bg-red-50 p-5 text-sm text-red-700">{error}</div>:<div className="overflow-x-auto"><table className="w-full min-w-[850px]"><thead className="bg-slate-50 text-xs uppercase tracking-wider text-slate-400"><tr><th className="px-7 py-4 text-left">Vehicle</th><th className="px-6 py-4 text-left">Driver</th><th className="px-6 py-4 text-left">Type</th><th className="px-6 py-4 text-left">Capacity</th><th className="px-6 py-4 text-left">Status</th><th className="px-6 py-4 text-right">Actions</th></tr></thead><tbody>{loading?<tr><td colSpan={6} className="p-14 text-center text-sm text-slate-500">Loading fleet...</td></tr>:filtered.length===0?<tr><td colSpan={6} className="p-14 text-center"><Truck className="mx-auto text-slate-300" size={34}/><p className="mt-3 font-semibold text-slate-700">{vehicles.length?"No vehicles match your search":"Your fleet is empty"}</p><p className="mt-1 text-sm text-slate-400">{vehicles.length?"Try a different search term.":"Add your first vehicle to start managing the fleet."}</p></td></tr>:filtered.map(v=><tr key={v.registration} className="border-t border-slate-100 hover:bg-slate-50/70"><td className="px-7 py-5"><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-xl bg-indigo-50"><Truck size={18} className="text-indigo-600"/></div><div><p className="font-semibold text-slate-800">{v.model}</p><p className="font-mono text-xs text-slate-400">{v.registration}</p></div></div></td><td className="px-6 text-sm text-slate-500">Unassigned</td><td className="px-6 text-sm text-slate-600">{v.type}</td><td className="px-6 text-sm text-slate-600">{v.capacity}</td><td className="px-6"><StatusBadge status={v.status}/></td><td className="px-6"><div className="flex justify-end gap-1"><button onClick={()=>onView(v)} className="rounded-lg p-2 text-slate-400 hover:bg-indigo-50 hover:text-indigo-600" title="View"><Eye size={17}/></button><button onClick={()=>onEdit(v)} className="rounded-lg p-2 text-indigo-600 hover:bg-indigo-50" title="Edit"><Pencil size={17}/></button><button disabled={deleting===v.registration} onClick={()=>del(v)} className="rounded-lg p-2 text-red-500 hover:bg-red-50 disabled:opacity-50" title="Delete"><Trash2 size={17}/></button></div></td></tr>)}</tbody></table></div>}</div>}
